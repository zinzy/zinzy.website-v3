[build]
  command = "python3 -m venv scripts/.venv && scripts/.venv/bin/pip install -q -r scripts/requirements.txt && scripts/.venv/bin/python scripts/dither_photos.py --limit 5 && hugo"
  publish = "public"
  functions = "netlify/functions"

[build.environment]
  HUGO_VERSION = "0.145.0"